<Page
    x:Class="Worldescape.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Worldescape"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" x:Name="TopButtonHolder">

            <Button  
                Style="{StaticResource MaterialDesign_Button_Style}"
                Content="Login" 
                Margin="10"  
                FontSize="20"
                x:Name="ChildWindowButton"
                Height="50"
                MinWidth="100" 
                Click="ChildWindowButton_Click">
            </Button>

            <Button   
                Style="{StaticResource MaterialDesign_Button_Style}"
                Content="Craft" 
                Margin="10"  
                FontSize="20"
                x:Name="CraftButton"
                Height="50"
                MinWidth="100" 
                Click="CraftButton_Click">
            </Button>

            <Button   
                Style="{StaticResource MaterialDesign_Button_Style}"
                Content="Move" 
                Margin="10"  
                FontSize="20"
                x:Name="MoveButton"
                Height="50"
                MinWidth="100" 
                Click="MoveButton_Click">
            </Button>

            <StackPanel x:Name="LastConstructHolder"></StackPanel>

        </StackPanel>

        <ScrollViewer 
            Grid.Row="1"
            x:Name="ScrollViewer_World"
			HorizontalScrollBarVisibility="Auto"			
			IsTabStop="False"			
			VerticalScrollBarVisibility="Auto">

            <Canvas
                x:Name="Canvas_root"  
                Width="8000"
			    Height="8000"
                Background="Transparent"
                PointerPressed="Canvas_root_PointerPressed"
                AllowDrop="True">
            </Canvas>

        </ScrollViewer>


    </Grid>

</Page>
